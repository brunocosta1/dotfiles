- default:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.Xresources2: 
    ~/.vim: 
    ~/.vimrc: vimrc
        relink: true
    ~/.zshrc: zshrc
        force: true
    ~/.config: config
        glob: true
        path: config/*
        relink: true
- create:
    - ~/.vim/undo-history

- shell:
    - [git submodule update --init --recursive, Installing submodules]



